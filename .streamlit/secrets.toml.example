# Copy this file to secrets.toml and fill in your own values.
# NEVER commit secrets.toml to version control.

[passwords]
admin  = "replace_with_strong_manager_password"
staff1 = "replace_with_strong_staff_password"

[cookies]
key = "replace_with_a_random_string_of_at_least_32_characters"
